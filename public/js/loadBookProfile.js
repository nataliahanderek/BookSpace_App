/*
const bookProfileButtons = document.querySelectorAll(".goToBookProfile");

function showBookProfile() {
    const clickTitle = this;
    const container = clickTitle.parentElement.parentElement;
    const book_id = container.getAttribute("book_id");

    fetch(`/bookprofile/${book_id}`);
}

bookProfileButtons.forEach(button => button.addEventListener("click", showBookProfile));*/
